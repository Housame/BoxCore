@*@*@model SlideshowVM

<style>

    html {
        height: 100%;
    }

    body {
        color: black;
        padding: 0px;
        height: 100vh;
        padding: 0px;
        margin: 0px;
    }

    .carousel-inner {
        padding: 0px;
        margin: 0px;
        height: 100vh;
    }

    .events-carousel-body {
        width: 100% !important;
        height: 100% !important;
    }

    .events-carousel, .active {
        height: 100% !important;
    }

    #mainDiv {
        padding: 0px;
    }

    .events-carousel-wrapper {
        padding: 0px;
        margin-top: 80px;
    }

    .item {
        padding-top: 80px;
        height: 100vh;
        margin: 0 auto;
        padding-bottom: 80px;
    }

    .bc-system-footer {
        position: absolute;
        right: 0;
        bottom: 0;
        left: 0;
    }

    #events-carousel-header {
        position: fixed;
        top: 0;
        left: 0;
        z-index: 999;
        width: 100%;
        height: 80px;
    }

    .carousel-caption {
        padding: 0px;
    }

    h1 {
        margin: 0px;
    }

    h5 {
        color: darkred;
    }

    td{
        font-size: 8px;
    }

</style>

@*<div id="slideShowContainer">

    <!-- Competition Title -->
    <div id="events-carousel-header" class="bc-system-header">
        <h2>@Model.CompetitionName</h2>
    </div>

    <!-- Body -->
    <div style="background-color:white;" id="events-carousel-body" class="carousel slide" data-ride="carousel" data-interval="5000">
        <!-- Wrapper for slides -->
        <div class="carousel-inner" role="listbox">

            <div class="item active">

                <h5><b>@Model.SubCompNames[0]</b></h5>*@

                @*<div class="container-fluid">
                    <div class="row">
                        <div class="col-sm-6">

                        </div>
                        <div class="col-sm-6">

                        </div>
                    </div>
                </div>*@

                @*<table id="example" class="table table-striped table-bordered" cellspacing="0" style="width:100%;">
                    <thead>
                        <tr>
                            <th>#</th>
                            @foreach (var eventName in Model.leaderboardResult[0].EventNames)
                            {
                                <th style="text-align:center;">@eventName</th>
                            }
                        </tr>
                    </thead>
                    <tbody>

                        @{int index = 1; }
                        @foreach (var teamResult in Model.leaderboardResult[0].TeamResults)
                        {
                            <tr class="result-row" data-value="@index">
                                @if (index < 2)
                                {
                                    <td class="ranking">@index</td>
                                }

                                else
                                {
                                    @if (Model.leaderboardResult[0].TeamResults[index - 2].TotalScore == teamResult.TotalScore)
                                    {
                                        <td class="ranking">-</td>
                                    }
                                    else
                                    {
                                        <td class="ranking">@index</td>
                                    }
                                }
                                <td>@teamResult.Name</td>
                                @foreach (var score in teamResult.Score)
                                {
                                    if (score != null)
                                    {
                                        <td>@score</td>
                                    }
                                    else
                                    {
                                        <td>Inga resultat ännu ...</td>
                                    }
                                }
                                <td class="tot-score">@teamResult.TotalScore</td>
                            </tr>
                            index++;
                        }
                    </tbody>
                </table>

            </div>

            @foreach (var leaderboard in Model.leaderboardResult.Skip(1))
            {

                <div class="item">

                    <h5><b>@Model.SubCompNames[Model.leaderboardResult.IndexOf(leaderboard)]</b></h5>

                    <table id="example" class="table table-striped table-bordered" cellspacing="0" style="width:100%;">
                        <thead>
                            <tr>
                                <th>#</th>
                                @foreach (var eventName in leaderboard.EventNames)
                                {
                                    <th style="text-align:center;">@eventName</th>
                                }
                            </tr>
                        </thead>
                        <tbody>
                            @{ int i = 1;}
                            @foreach (var teamResult in leaderboard.TeamResults)
                            {
                                <tr>
                                    @if (i < 2)
                                    {
                                        <td class="ranking">@i</td>
                                    }

                                    else
                                    {
                                        @if (leaderboard.TeamResults[i - 2].TotalScore == teamResult.TotalScore)
                                        {
                                            <td class="ranking">-</td>
                                        }
                                        else
                                        {
                                            <td class="ranking">@i</td>
                                        }
                                    }
                                    <td>@teamResult.Name</td>
                                    @foreach (var score in teamResult.Score)
                                    {
                                        if (score != null)
                                        {
                                            <td>@score</td>
                                        }
                                        else
                                        {
                                            <td>Inga resultat ännu ...</td>
                                        }
                                    }
                                    <td class="tot-score">@teamResult.TotalScore</td>
                                </tr>
                                i++;
                            }
                        </tbody>
                    </table>
                </div>
            }

        </div>
    </div>
    <div class="bc-system-footer"><a ></a></div>

</div>*@


@section Scripts{

    <script>

        $(document).ready(function () {
            console.log('hello');
            //$("#example tr").each(function () {
            //    var score = $(this).find('td .tot-score').text();
            //    if (score !== null) {

            //        console.log(score);
            //        $(this).find('td .ranking').text(score);
            //    }
            //    else {
            //        console.log('fuck');
            //    }
            //});
            var slideshowContainer = $('#mainDiv');
            $('body').empty().html(slideshowContainer);

        });

    </script>

}

@*<div id="">
        <a id="down"></a>
        <div class="">
            <div class="">
                <div id="events-carousel-header" class="bc-system-header">
                    <h2>@Model.CompetitionName</h2>
                </div>
                <div id="events-carousel-body" class="carousel slide text-center events-carousel-body events-carousel" data-ride="carousel" data-interval="5000">

                    <div class="carousel-inner">

                        <div class="item active">

                            <p><b>@Model.SubCompNames[0]</b></p>

                            <table id="example" class="table table-striped table-bordered" cellspacing="0" style="width:100%;">
                                <thead>
                                    <tr>
                                        @foreach (var eventName in Model.leaderboardResult[0].EventNames)
                                        {
                                            <th style="text-align:center;">@eventName</th>
                                        }
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var teamResult in Model.leaderboardResult[0].TeamResults)
                                    {
                                        <tr>
                                            <td>@teamResult.Name</td>
                                            @foreach (var score in teamResult.Score)
                                            {
                                                if (score != null)
                                                {
                                                    <td>@score</td>
                                                }
                                                else
                                                {
                                                    <td>-</td>
                                                }
                                            }
                                            <td>@teamResult.TotalScore</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>

                        </div>

                        @foreach (var leaderboard in Model.leaderboardResult.Skip(1))
                        {

                            <div class="item">

                                <p><b>@Model.SubCompNames[Model.leaderboardResult.IndexOf(leaderboard)]</b></p>

                                <table id="example" class="table table-striped table-bordered" cellspacing="0" style="width:100%;">
                                    <thead>
                                        <tr>
                                            @foreach (var eventName in leaderboard.EventNames)
                                            {
                                                <th style="text-align:center;">@eventName</th>
                                            }
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var teamResult in leaderboard.TeamResults)
                                        {
                                            <tr>
                                                <td>@teamResult.Name</td>
                                                @foreach (var score in teamResult.Score)
                                                {
                                                    if (score != null)
                                                    {
                                                        <td>@score</td>
                                                    }
                                                    else
                                                    {
                                                        <td>-</td>
                                                    }
                                                }
                                                <td>@teamResult.TotalScore</td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        }
                    </div>
                </div>
                <div class="bc-system-footer"></div>
            </div>
        </div>
    </div>*@
