@model UserDisplayVM[]

@if (Model != null)
{
    @foreach (UserDisplayVM user in Model)
    {
        <a asp-controller="UserClient" asp-action="EditUser" asp-route-id="@user.Id" class="list-group-item list-group-item-action">
            @user.FirstName @user.LastName
        </a>
    }
}